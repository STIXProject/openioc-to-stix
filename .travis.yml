language: python

python:
  - "2.6"
  - "2.7"

install:
  - pip install -U pip setuptools
  - pip install tox-travis

script:
  - tox

notifications:
  email:
    - stix-commits-list@groups.mitre.org
